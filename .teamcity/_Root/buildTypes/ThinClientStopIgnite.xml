<?xml version="1.0" encoding="UTF-8"?>
<template xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" uuid="5d556595-44f5-4127-b18e-a5fbbec1fec0" xsi:noNamespaceSchemaLocation="https://www.jetbrains.com/teamcity/schemas/2021.1/project-config.xsd">
  <name>Thin Client: Stop Ignite</name>
  <description>Stops all Apache Ignite nodes</description>
  <settings>
    <parameters />
    <build-runners>
      <runner id="RUNNER_12" name="Stop Ignite node" type="simpleRunner">
        <parameters>
          <param name="script.content"><![CDATA[#!/usr/bin/env bash
set -o nounset; set -o errexit; set -o pipefail; set -o errtrace; set -o functrace
set -x


# Stop node by it's PID (if it was not killed previously)
ps ax | grep ignite.sh | sed '$d' | awk {'print $1'} | while read pid; do
    kill -9 ${pid} || true
done]]></param>
          <param name="teamcity.step.mode" value="execute_always" />
          <param name="use.custom.script" value="true" />
        </parameters>
      </runner>
    </build-runners>
    <vcs-settings />
    <requirements />
    <build-triggers />
    <cleanup />
  </settings>
</template>

