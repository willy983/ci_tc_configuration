<?xml version="1.0" encoding="UTF-8"?>
<template xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" uuid="47a70e7f-df08-42bc-8a11-cc947a2fcc4b" xsi:noNamespaceSchemaLocation="https://www.jetbrains.com/teamcity/schemas/2021.1/project-config.xsd">
  <name>Run Python tests (SSL)</name>
  <description>=&gt; Requires Run Python tests (Basic)</description>
  <settings>
    <parameters>
      <param name="IGNITE_INIT_TIME" value="" spec="text display='hidden' validationMode='any'" />
      <param name="VCS_ROOT_PYTHON" value="" spec="text display='hidden' validationMode='any'" />
      <param name="env.PYTHON_TEST_CONFIG_PATH" value="" spec="text display='hidden' validationMode='any'" />
    </parameters>
    <build-runners>
      <runner id="RUNNER_91" name="Start Ignite node with SSL" type="simpleRunner">
        <parameters>
          <param name="script.content"><![CDATA[#!/usr/bin/env bash
set -o nounset; set -o errexit; set -o pipefail; set -o errtrace; set -o functrace
set -x


# Run Apache Ignite node
bash %system.teamcity.build.checkoutDir%/bin/ignite.sh %env.PYTHON_TEST_CONFIG_PATH%/ssl.xml &
sleep %IGNITE_INIT_TIME%]]></param>
          <param name="teamcity.step.mode" value="default" />
          <param name="use.custom.script" value="true" />
        </parameters>
      </runner>
      <runner id="RUNNER_92" name="Run Python tests - SSL" type="simpleRunner">
        <parameters>
          <param name="script.content"><![CDATA[#!/usr/bin/env bash
set -o nounset; set -o errexit; set -o pipefail; set -o errtrace; set -o functrace
set -x


# Run tests
python3 ./setup.py pytest --addopts="--use-ssl=True --ssl-certfile=%env.PYTHON_TEST_CONFIG_PATH%/ssl/client_full.pem" || exit 1]]></param>
          <param name="teamcity.build.workingDir" value="%VCS_ROOT_PYTHON%" />
          <param name="teamcity.step.mode" value="default" />
          <param name="use.custom.script" value="true" />
        </parameters>
      </runner>
      <runner id="RUNNER_93" name="Run Python tests - SSL with keyfile password" type="simpleRunner">
        <parameters>
          <param name="script.content"><![CDATA[#!/usr/bin/env bash
set -o nounset; set -o errexit; set -o pipefail; set -o errtrace; set -o functrace
set -x


# Run tests
python3 ./setup.py pytest --addopts="--use-ssl=True --ssl-certfile=%env.PYTHON_TEST_CONFIG_PATH%/ssl/client_with_pass_full.pem --ssl-keyfile-password=654321" || exit 1]]></param>
          <param name="teamcity.build.workingDir" value="%VCS_ROOT_PYTHON%" />
          <param name="teamcity.step.mode" value="default" />
          <param name="use.custom.script" value="true" />
        </parameters>
      </runner>
      <runner id="RUNNER_94" name="Stop Ignite node with SSL" type="simpleRunner">
        <parameters>
          <param name="script.content"><![CDATA[#!/usr/bin/env bash
set -o nounset; set -o errexit; set -o pipefail; set -o errtrace; set -o functrace
set -x


# Stop node by it's PID (if it was not killed previously)
ps ax | grep ignite.sh | sed '$d' | awk {'print $1'} | while read pid; do
    kill -9 ${pid} || true
done]]></param>
          <param name="teamcity.step.mode" value="execute_always" />
          <param name="use.custom.script" value="true" />
        </parameters>
      </runner>
    </build-runners>
    <vcs-settings />
    <requirements />
    <build-triggers />
    <cleanup />
  </settings>
</template>

