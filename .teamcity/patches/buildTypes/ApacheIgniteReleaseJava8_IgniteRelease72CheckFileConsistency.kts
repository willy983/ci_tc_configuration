package patches.buildTypes

import jetbrains.buildServer.configs.kotlin.v2019_2.*
import jetbrains.buildServer.configs.kotlin.v2019_2.ui.*

/*
This patch script was generated by TeamCity on settings change in UI.
To apply the patch, change the buildType with id = 'ApacheIgniteReleaseJava8_IgniteRelease72CheckFileConsistency'
accordingly, and delete the patch script.
*/
changeBuildType(RelativeId("ApacheIgniteReleaseJava8_IgniteRelease72CheckFileConsistency")) {
    dependencies {
        remove(RelativeId("Releases_ApacheIgniteMain_ReleaseBuild")) {
            artifacts {
                buildRule = lastSuccessful("ignite-%IGNITE_VERSION%")
                artifactRules = "release*.zip!svn/vote** => raw/cur"
            }
        }

        add(RelativeId("Releases_ApacheIgniteMain_ReleaseBuild_1")) {
            artifacts {
                buildRule = lastSuccessful()
                artifactRules = "release*.zip!svn/vote** => raw/cur"
            }
        }

    }
}
